# Demo capture internals

The canonical flow lives in `Tests/DockyardDemoUITests/DockyardDemoUITests.swift`. Clip boundaries and encoding settings live in `scripts/capture-website-demo.sh`. The recorder implementation is `Sources/DemoRecorder/main.swift`.
